extends layout

block content
  h1 Users index
  a(href="/new") Create new

  if message
    h3 Message: #{message}

  ul
    each user in users
      li
        a(href="/" + user.id) #{user.id} - #{user.email}
